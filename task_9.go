package main

import "fmt"

func main() {
	// var num float64 — объявление переменной для вещественного числа.
	var num float64
	// fmt.Scan(&num) — считывает введенное число с плавающей точкой.
	fmt.Scan(&num)
}
